# macOS
.DS_Store
._*

# Claude Code local settings — machine-specific, not shared
.claude/settings.local.json

# Build output (regenerated by ../../private/GuideGen/store/RUNBOOK.md step 2).
# Lands in the parent GuideGen/ folder, so it is outside this repo anyway.
*.zip
